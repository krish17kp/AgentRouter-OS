{
  "loop_version": 2,
  "last_updated": "2026-09-10",
  "current_phase": "Branch consolidation + RC->main promotion attempt COMPLETE. PR #14 (cleanup) and PR #15 (dynamic-skill-discovery + TASK-020) merged into RC (now 62d5289). PR #16 (RC->main) open; real enforce-release-gate ran and failed only on context_band_accuracy (0.6667<0.90) -- left open, not merged, per the human-annotation-only gate policy. PR #13 (TASK-019 plugin installer hardening) remains DRAFT, untouched, now 11 commits behind RC with real future conflicts in diagnostics.py and tests/conftest.py (confirmed via git merge-tree).",
  "current_objective": "Resume TASK-019 from PR #13's actual current state (see loop/tasks/TASK-019-plugin-installer-hardening/ if present, and gh pr view 13); expect a real rebase conflict in agentrouter/diagnostics.py and tests/conftest.py against the new RC, not a mechanical one. Separately: PR #16 stays open until the two-human annotation round (TASK_012_OWNER_ACTIONS.md) closes the context-band gate.",
  "status": "IN_PROGRESS",
  "branch_topology": {
    "main": "stable, untouched (602321a) -- never merge without owner approval; PR #16 open against it, blocked on context_band_accuracy",
    "release/agentrouter-v0.5-rc1": "RC @ 62d5289 (PR #14 + PR #15 merged this session); NOT RELEASE READY (context_band 0.6667 < 0.90, unchanged) -- this is the ONLY failing gate per the real enforce-release-gate run on PR #16",
    "task/repo-cleanup-architecture-ponytail": "MERGED to RC (PR #14, merge commit 1ae3155); deleted local, remote deletion blocked by pre_tool_guard (owner action)",
    "task/dynamic-skill-discovery": "MERGED to RC (PR #15, merge commit 62d5289); deleted local, remote deletion blocked by pre_tool_guard (owner action)",
    "task/TASK-011-context-band-data": "MERGED to RC (PR #2) and DELETED local+remote; all commits preserved",
    "task/TASK-012-annotation-operations": "MERGED to RC (PR #3, merge commit 0ddf401) and DELETED local+remote",
    "task/TASK-013-catalog-provenance": "MERGED to RC (PR #4, merge commit c8fe429) and DELETED local+remote; provenance block + deprecation reconciliation deferred to TASK-015",
    "task/TASK-014-ci-release-semantics": "MERGED to RC (PR #5, merge commit b62ffd7) and DELETED local+remote 2026-08-08",
    "task/TASK-015-trusted-catalogs": "MERGED to RC (PR #6, merge commit 8661629) and DELETED local+remote 2026-08-08",
    "mutation-kill-safety": "DELETED (local+remote) 2026-07-31 — PR #1 closed, fully superseded by tests/test_mutation_kills.py",
    "task/TASK-016-verified-host-states": "MERGED to RC (PR #7, merge commit 5847c22) and DELETED local+remote 2026-08-08",
    "task/TASK-018A-api-contract-sdk-compatibility": "MERGED to RC (PR #9, merge commit c11ddec) and DELETED local+remote",
    "task/TASK-018B-reliability-load-lab": "MERGED to RC (PR #10, merge commit ff06910) and DELETED local+remote 2026-08-20",
    "task/TASK-018C-operations-observability": "MERGED to RC (PR #11, merge commit d81ad94) and DELETED local+remote 2026-08-20"
  },
  "release_branch": "release/agentrouter-v0.5-rc1",
  "rc_push_authorization": "GRANTED by owner 2026-07-31 — RC pushed NOT-RELEASE-READY; context_band_accuracy 0.6667 < 0.90 gate remains honestly failed; no merge/tag/publish/deploy; gate unchanged.",
  "repo_sha_at_takeover": "602321af91ff298c0d5b59d6d36c24f845557fc1",
  "canonical_release_ready": false,
  "in_progress": [
    "Phase 3 local engineering via the full graph-aware loop. Done+merged: TASK-013 catalog freshness status + rollback (PR #4), TASK-014 CI release-gate report/enforce split (PR #5). Next: TASK-015 trusted catalogs (file-level provenance block, deprecation reconciliation, atomic refresh, rollback hardening, provider doctor/status), then verified execution-host states, provider diagnostics/onboarding, benchmark routing infra, customer docs, observability/runbooks, API/SDK compat+load testing. External: the human annotation round (TASK-012 tooling shipped) needs two real annotators + adjudicator per TASK_012_OWNER_ACTIONS.md.",
    "ENV BLOCKER RESOLVED 2026-08-08: local pytest now runs cleanly on this machine (595 passed, 3 skipped in 18.4s; ruff clean; bandit 0). CI remains an independent cross-platform authority (3.10-3.13 + Windows) but is no longer the only usable regression signal."
  ],
  "completed_iter25": [
    "TASK-015 trusted catalogs MERGED to RC (PR #6 -> merge commit 8661629; task branch deleted local+remote). Closes TASK-013's deferred scope: file-level provenance block (provider, source_url, fetched_at UTC, count, tool_version, cli_args) ignored by the registry loader; atomic refresh via tempfile.mkstemp + os.replace; candidate-deprecation reporting on refresh (report-only, never auto-deletes); rollback with non-destructive backup rotation + new `providers restore`; new `providers doctor` that schema-validates every generated catalog and exits 3 only on real corruption (not staleness).",
    "Independent security review of the TASK-015 diff: 0 CRITICAL, 3 MEDIUM, 2 LOW, all MEDIUM/LOW fixed with regression tests and re-verified against the reviewer's own reproductions. (1) Predictable temp filename + symlink-following writes let a pre-planted symlink redirect a catalog write outside the registry dir — fixed with mkstemp and an O_CREAT|O_EXCL exclusive-create backup. (2) doctor/status/route crashed with a traceback (exit 1) on a non-mapping YAML root or non-UTF-8 bytes — fixed with a typed CatalogError + registry._load_yaml UnicodeDecodeError catch; all three now fail cleanly with exit 3. (3) Backup rotation used 1-second granularity and overwrote history when two rollbacks landed in the same second — fixed with a nanosecond key + collision probe (5 rapid cycles now keep 5 backups). LOW: provider-id allowlist for rollback/restore; control characters stripped from provenance before display.",
    "GIT GUARDRAIL REPAIRED under explicit owner authorization. .claude/hooks/pre_tool_guard.py had blocked git add/commit/push unconditionally, contradicting command.md section 14 and the AGENTS.md git policy and making the documented per-task loop impossible. Now branch-aware: add/commit only on task/* branches; push only from a task/* branch for that same branch, never forced, never targeting a protected branch. Writes to main and release/* stay refused, as do force push, remote-branch deletion, tags, history rewriting, reset --hard, git clean, recursive deletes, deployment, publication, shell=True, permission bypass and secret printing. 33 hook tests cover the allow/block matrix, incl. that shell redirections are not parsed as push refspecs.",
    "Verification: 629 passed / 3 skipped locally; 33 hook tests; ruff check + format clean; bandit 0; pip-audit no known vulnerabilities; clean-wheel install outside the repo exercised providers status/doctor/rollback/restore + route incl. the corrupt-catalog exit-3 path. Post-merge RC CI green: CI matrix 3.10-3.13 + test-windows + build-smoke + release-readiness-report, Security, and Critical Mutation Testing all success."
  ],
  "completed_iter24": [
    "TASK-014 CI release-gate semantics MERGED to RC (PR #5 -> merge commit b62ffd7; task branch deleted local+remote). Split the always-on enforcing release-gate into release-readiness-report (push/PR/dispatch; non-enforcing; prints Release-ready YES/NO + the real context_band score; uploads artifact) and enforce-release-gate (RC->main PR, release tag, or explicit workflow_dispatch input only; runs eval run --all --require-release-ready with no continue-on-error, no lowered thresholds). Ordinary RC/PR CI is now green; the honest gate still blocks promotion.",
    "PR #5 checks verified green (test 3.10-3.13, test-windows, build-smoke, Security scan, release-readiness-report all pass; enforce-release-gate and live-smoke correctly SKIPPED on a task->RC PR by design), marked ready for review, merged, branch deleted."
  ],
  "completed_iter23": [
    "TASK-013 catalog provenance/freshness increment MERGED to RC (PR #4 -> merge commit c8fe429; task branch deleted local+remote). agentrouter/catalog_ops.py (read_status/list_generated freshness vs registry.STALE_AFTER_DAYS; rollback with .bak backup, idempotent, manual registry untouched) + CLI `providers status` / `providers rollback <provider>`. tests/test_catalog_ops.py (9 tests). Structured file-level provenance block and deprecation reconciliation were explicitly deferred to keep this increment low-risk and independently verifiable — picked up by TASK-015."
  ],
  "completed_iter22": [
    "TASK-012 human annotation operations MERGED to RC (PR #3 -> merge commit 0ddf401; task branch deleted). agentrouter/annotation/packs.py + dataset {pack,progress,export,adjudication-pack} and a resumable annotate (per-item save; Ctrl-C/EOF clean pause). Blinded independent packs; label-free progress; disagreements-only adjudication pack with no auto-adjudication; JSONL+CSV export. Operator docs ANNOTATOR_A/B, ADJUDICATOR, TASK_012_OWNER_ACTIONS. tests/test_annotation_packs.py. CI green except honest release-gate. Env: local pytest killed for multi-second runs this session; CI was the full-suite authority."
  ],
  "completed_iter21": [
    "TASK-011 context-band annotation program MERGED to RC (PR #2 -> merge commit 35e616e; all 4 commits preserved; task branch deleted local+remote). agentrouter/annotation/ package: schema, deterministic unlabelled candidate generation (63 prompts, all ten categories, 0 frozen-holdout leaks), dedup/near-dup + cross-set leakage, two-annotator adjudication, leakage-safe train/dev/holdout splits, versioned manifests, rules/learned/hybrid comparison (accuracy/macro-F1/per-band recall/bootstrap CI/ECE), optional Graphify impact with text-only fallback, and the `agentrouter dataset` CLI. docs/CONTEXT_BAND_ANNOTATION.md. 26 annotation tests; full suite 583+ pass; clean-wheel verified (packaged pool loads). Focused pre-merge review found + fixed 2 issues (build LeakageError -> clean exit 2; annotate help wording).",
    "CI on RC @ 35e616e: test matrix 3.10-3.13 + test-windows + build-smoke + Security + Critical Mutation Testing GREEN; only release-gate RED (honest context_band 0.6667<0.90). main untouched.",
    "State docs reconciled from actual GitHub state: LOOP_STATE, LOOP_LOG, PROJECT_STATUS, CODEX_HANDOFF, RELEASE_READINESS, QUALITY_DASHBOARD."
  ],
  "completed_iter20": [
    "TASK-010b critical-module mutation hardening CLOSED. Full Linux campaign (mutmut 3.6.0, 923 mutants): overall 0.9837; safety_policy_execution 0.985 (>=0.95); routing_engine 0.9815 (>=0.85); completeness + all-reviewed gates green; no safety/auth/policy/execution-bypass survivor. 15 remaining survivors all provably-equivalent and allowlisted. Added tests/test_mutation_kills.py (~78 tests). Extracted cli.execute() gate logic into undecorated _execute() so mutmut can instrument it (mutmut skips @app.command funcs). Fixed a flaky 200ms Hypothesis deadline on injected-clock property tests. Iterated locally via Docker container 'armut' (mutmut cannot run on Win/py3.13).",
    "CI on RC head fa0a203: Critical Mutation Testing GREEN, Security GREEN, test matrix 3.10-3.13 + test-windows + build-smoke GREEN; only release-gate RED (honest context-band 0.6667<0.90).",
    "Branch hygiene: PR #1 closed with supersession proof; mutation-kill-safety deleted local+remote; worktree removed. Five branch files (factories.py + 4 mutation-hardening test files) proven fully superseded — RC gate passes without them."
  ],
  "completed_iter19": [
    "TASK-009 decision A — dev-only LEARNED context-band classifier built (multinomial LR, 14 interpretable regex features, LOOCV 0.9583, C=1.0). Runtime is pure-Python softmax over JSON weights (no sklearn/network, deterministic); shared feature extractor prevents train/serve skew. Artifact agentrouter/benchmarks/context_band_model_v1.json packaged in the wheel and verified loading from a fresh pip install.",
    "Frozen holdout evaluated ONCE across 3 configs (config fixed a priori, no holdout tuning/selection): rules-only 0.6667 macroF1 0.6792 (recall S/M/L 0.733/0.600/0.667); learned-only 0.6889 macroF1 0.6613 (0.800/0.333/0.933 — lifts large, collapses medium); hybrid(0.45) 0.6444 macroF1 0.6188. CIs overlap heavily; learned NOT significantly better than rules and has lower macro-F1.",
    "DECISION: ship rules-active (_USE_LEARNED_BAND=False) — best macro-F1, balanced recall, explainable, no significant loss. Learned model retained as tested, flag-gated artifact with rule fallback. PROVEN CEILING: 24 dev cases cannot generalize to 0.90; closing the gap honestly needs a larger human-labeled dev set or real-file signals, not more hand-rules.",
    "Regression: 481 collected — 480 passed + 1 env-skip (otel), incl 7 new tests/test_context_model.py; ruff clean; bandit 0 (under -c pyproject.toml). eval run --all grade 98.23, 6/7 gates PASS, context_band gate FAIL (honest ceiling). One pre-existing plugin backup test is order-flaky on Windows (passes isolated), unrelated to TASK-009.",
    "Graphify updated post-implementation: 2309 nodes / 4319 edges / 239 communities. context_model.py is a leaf module (stdlib-only imports); classifier depends one-way; zero reverse edges = no cycle.",
    "Independent gates: release-auditor verdict FAIL-to-release / correctly HELD (all claims reproduced, safe to hold unpushed). security-reviewer-arros: no CRITICAL/HIGH; one LOW fixed — load_model() now catches TypeError to honor 'any error -> None' contract, covered by test_malformed_model_returns_none.",
    "Doc reconcile: loop/QUALITY_GATES.yaml refreshed (achieved 0.5778->0.6667, grade 98.32->98.23); study doc + state files corrected to 7 tests / 480-pass."
  ],
  "completed_iter18": [
    "TASK-008 safe plugin uninstall — INDEPENDENT RE-AUDIT PASS: 49 tests pass; live sandbox cycle verified (managed file removed, empty owned dir removed, non-empty dir preserved, unrelated user file preserved, idempotent, reinstall works, state dir cleaned). No repair needed.",
    "TASK-009 held-out context — honest dev-derived generalization: classifier M_ACT_ON_EXISTING + _DEFINITE_EXISTING_RE; holdout 0.5778->0.6667 acc, macro-F1 ->0.6792, medium recall 0.267->0.60; dev stays 1.0; one holdout measurement, no holdout tuning. Still < 0.90 (genuine gap).",
    "TASK-010 local validation complete (see in_progress for CI-only remainder).",
    "Baseline B311 bandit regression fixed (nosec, deterministic resampling) -> bandit 0.",
    "Graphify activated: official graphifyy 0.9.30 (uv tool) confirmed; graph built via CLI (2271 nodes / 4262 links / 238 communities / 281 files); validated (all prod modules present, 0 excluded-dir leaks, 0 secrets, calls/imports/inherits edges); docs/GRAPHIFY.md + docs/GRAPHIFY_BASELINE.md written; graphify-out/ gitignored; .claudeignore added; no global .claude skill install (CLI-direct, hooks untouched).",
    "Autonomy codified in AGENTS.md, command.md, .claude/skills/production-loop/SKILL.md: routine reversible local tasks auto-continue with no confirmation; owner asked only for RC-push-with-failing-gate / merge / publish / deploy / paid / credentials / destructive."
  ],
  "current_measurements": {
    "python_full_baseline": "427 passed before final audit repairs",
    "plugin_focused": "48 passed",
    "context_holdout_focused": "9 passed",
    "mutation_harness_focused": "8 passed",
    "typescript": "typecheck clean; 18/18 tests with the live app, 0 skipped; a skipped live suite fails CI under AGENTROUTER_REQUIRE_LIVE=1",
    "hooks": "33 passed",
    "evaluation": "98.23/100; 6/7 gates pass; canonical release readiness NO",
    "context_holdout": "SHIPPED rules-active accuracy 0.6667, macro-F1 0.6792 (recall S/M/L 0.733/0.600/0.667). Learned-only 0.6889/0.6613 and hybrid 0.6444/0.6188 measured once; none reach 0.90; learned not significantly better and lower macro-F1. Proven dataset ceiling with 24 dev cases.",
    "mutation": "FIRST REAL LINUX SCORE (2026-07-31, run on release/agentrouter-v0.5-rc1): overall 0.6054 (denom 745); safety_policy_execution 0.5701 vs 0.95 target; routing_engine 0.6512 vs 0.85; 294 unreviewed survivors (engine 113, hosts 72, cli 70, controls 25, server.limits 13, safety 1). Tool ran cleanly (no crash) — the FAIL is a real test-quality gap, not a harness failure. Allowlisting cannot fix it (survivors stay in the denominator); reaching gates needs ~225 new mutation-killing tests, verifiable only via ~40-min Linux CI cycles (mutmut cannot run on the Win/py3.13 dev box). Tracked as TASK-010b (multi-session).",
    "ci_status_fa0a203": "CI: test 3.10/3.11/3.12/3.13 + test-windows + build-smoke GREEN; release-gate RED by design (context_band 0.6667<0.90) — the ONLY red. Security GREEN. Critical Mutation Testing GREEN (TASK-010b closed).",
    "mutation_final": "PASS at d81ad94: overall 0.986; safety_policy_execution 0.9879 (>=0.95); routing_engine 0.9815 (>=0.85); 0 unreviewed survivors; all five gates PASS",
    "bandit": "0 issues (fixed B311 nosec in evaluation/context_bands.py bootstrap resampling)",
    "graphify": "official graphifyy v0.9.30 via uv tool; post-impl graph 2309 nodes / 4319 edges / 239 communities; context_model.py leaf module, no cycle",
    "python_full": "936 passed, 3 skipped; branch coverage 85.42% (gate 80%)",
    "clean_wheel": "context_band_model_v1.json packaged; loads + predicts from fresh pip install; shipped _USE_LEARNED_BAND=False confirmed in installed wheel",
    "contract": "9 operations; agentrouter contract check exit 0, 0 breaking / 0 risky / 0 additive; byte-identical when exported from a clean wheel install in an unrelated directory; CI also checks against the PR base contract so deleting the branch copy cannot turn a breaking change green",
    "graph": "graphifyy 0.9.47; 3623 nodes / 7084 edges / 309 communities from RC d81ad94; graphify-out/ is gitignored and local-only"
  },
  "release_blockers": [
    "context_band_accuracy 0.6667 is below the unchanged 0.90 frozen-holdout gate. Decision A tested a dev-only learned model + hybrid: PROVEN CEILING — none reach 0.90, learned not significantly better than rules (overlapping CIs) and has lower macro-F1. Closing to 0.90 honestly needs a larger human-labeled dev set or real-file context signals, NOT more hand-rules or holdout tuning. Shipped rules-active.",
    "real Linux mutmut score and survivor review await GitHub Actions",
    "final full local matrix and release-branch GitHub checks are pending"
  ],
  "external_or_owner_blocked": [
    "live provider catalog/access verification requires credentials and opt-in network use",
    "measured paid-model profiles require an inference budget",
    "hosted deployment, marketplace publication, beta feedback, and production operations require owner action"
  ],
  "historical_work_preserved": [
    "route controls, taxonomy, setup, safety hardening, API/Python SDK, evaluation framework, observability, rate limiting/idempotency, MCP, TypeScript SDK, SBOM/provenance, property tests",
    "historical task records remain under loop/tasks and loop/reports",
    "historical TASK-004 0.945 result is retained as in-sample evidence but no longer treated as canonical held-out readiness"
  ],
  "next_action": "Resume TASK-019 (plugin installer hardening) from PR #13's actual DRAFT state -- it is already in progress with several commits landed, do not restart. Rebase/merge onto current RC (62d5289) first; expect real conflicts in agentrouter/diagnostics.py and tests/conftest.py (confirmed via git merge-tree, not just GitHub's shallow mergeable flag), not mechanical ones. Continue reproduce-before-fixing as in EPIC TASK-018. Separately: PR #16 (RC->main) stays open, correctly blocked by the real enforce-release-gate on context_band_accuracy alone (0.6667<0.90, all 6 other gates pass) -- external/owner-blocked, the two-annotator round (TASK_012_OWNER_ACTIONS.md) remains the only honest path to the 0.90 context-band gate.",
  "completed_iter26": [
    "TASK-016 verified execution-host states MERGED to RC (PR #7 -> merge commit 5847c22; task branch deleted local+remote). hosts.py gained a readiness state vocabulary (missing/installed/configured/authenticated/authorized/degraded/unknown) plus a per-host remedy; `availability` keeps its exact prior meaning and is now derived from state. hosts list/doctor/show, the setup wizard and the execute refusal all report state + fix; hosts doctor ends with the cheapest concrete fix and exits non-zero only when no real host is ready. REST/MCP gained additive state/remedy and host_state/host_remedy keys. docs: new USER_GUIDE section 8 with the state table and the honesty note on `authorized`.",
    "BUG FIXED: host detection tested the API-key env var for truthiness, so a set-but-blank key (OPENAI_API_KEY='   ') reported available and `execute` would target a host that cannot authenticate. Now degraded/unavailable with a fix-it message, pinned by a parametrised test.",
    "HONESTY: `authorized` requires a live authorization check that is not implemented (command.md PHASE C lists opt-in live checks separately), so no offline path returns it - asserted by test. `authenticated` means a credential exists, never that it was accepted.",
    "Independent security review of the diff: 0 critical, 1 HIGH, 1 MEDIUM, 3 LOW, all fixed and regression-tested. HIGH: this task introduced the first filesystem access into detect_host and Path.exists()/is_dir() propagate EACCES/ENAMETOOLONG, so an untraversable HOME turned hosts doctor, route and the REST /v1/hosts endpoint into a traceback (unhandled 500) - a real regression, since detection could not raise before. MEDIUM: a blank env fallback outranked a working CLI login. LOW: registry-controlled required_command echoed unsanitized (ANSI forgery); a non-hermetic mutation-kill test; 'closest fix' was really first-in-list order. Reviewer verified zero file opens during detection, no credential in any output, and an exhaustive 4320-case differential showing 0 cases more permissive than before (912 strictly stricter).",
    "CI REPAIR: critical-modules failed on the first run - the new code added mutants the suite did not kill (safety_policy_execution 0.8895 vs 0.95, 72 unreviewed survivors). Repaired with 30 exact-value mutation-killing tests, NOT by lowering a threshold or allowlisting. Reproduced the campaign locally (mutmut 3.6.0, same flags, 1057 mutants) to confirm before pushing: overall 0.9858, safety_policy_execution 0.9877, routing_engine 0.9815, 0 unreviewed survivors, all five gates PASS. Post-merge RC CI green across CI, Critical Mutation Testing and Security.",
    "Verification: 684 passed / 3 skipped; 33 hook tests; ruff + format clean; bandit 0 with no new nosec; pip-audit clean; clean-wheel install outside the repo exercised hosts doctor/show, the blank-key path, the unreadable-HOME path and route."
  ],
  "completed_iter27": [
    "TASK-018A versioned HTTP API contract + semantic compatibility gate + SDK parity, implemented and verified on task/TASK-018A-api-contract-sdk-compatibility (not yet merged). agentrouter/contract.py exports a canonical byte-stable OpenAPI document to contracts/http/v1/openapi.json with provenance in a sibling manifest.json (deliberately not in the contract, so an unchanged API produces an unchanged file). `agentrouter contract export|check [--json] [--out]` exits 0 compatible / 1 breaking / 3 missing-or-untrustworthy baseline; a missing baseline is never a pass; export refuses to overwrite a breaking change and accepting one requires --accept-breaking --reason, appended to a history a later export cannot erase. New CI job api-compatibility; enforce-release-gate now needs it; ci.yml and security.yml pinned to permissions: contents: read.",
    "THE GATE WAS GUARDING NOTHING, TWICE — both fixed. (1) Four of nine operations were declared `-> dict`, so the exported contract said only 'an object' for their responses and a renamed or removed field on /v1/classify, /v1/route, /v1/decisions/{id} or /v1/execute/dry-run produced no diff at all. Now typed response envelopes with extra='allow', verified against live payloads so nothing is stripped from the wire (route 14 keys, decision 16, dry-run 7, classify 12 all preserved). (2) Auth was a bare X-API-Key Header parameter, indistinguishable in the export from any other optional header, so adding or removing authentication on an endpoint was invisible. Now a declared APIKeyHeader security scheme.",
    "SECURITY REVIEW — 2 HIGH on the pre-existing server, both real, both fixed with regression tests. (1) @app.exception_handler(Exception) was registered on Starlette's OUTERMOST middleware, above request_id_middleware, so it never ran: an unhandled fault was answered by ServerErrorMiddleware with no envelope and no X-Request-ID. Now converted inside request_id_middleware AND logged at ERROR with a redacted traceback, because catching it there also stops the ASGI server logging it - otherwise a leaky 500 would have become a silent one. (2) The RegistryError handler returned str(exc), which interpolates the registry path and the offending YAML source line; a registry is malformed at exactly the moment someone has pasted a credential into it, so this returned that credential to an UNAUTHENTICATED caller on /v1/models and /ready. Both now answer registry_unavailable with a fixed message, detail to the log only. /ready no longer catches RegistryError itself, so readiness and the /v1 routes report the same code for the same condition.",
    "A GATE YOU CAN EDIT IS NOT A GATE. An unresolvable, self-referential or remote $ref inlined as {} and made a schema look permissive, so a one-character edit to the committed baseline turned CI green - load_baseline now refuses such a document. $ref expansion is bounded by node and depth budgets (a fan-out document would otherwise OOM). Export refuses to follow a symlink or write outside the repo root. The acceptance history records only the changes the checker itself found, so it cannot assert a breaking change that never happened, and a later routine export cannot erase it.",
    "MY OWN TEST FOUND THE NEXT BUG: validate_refs re-walked every path, so a 26-level fan-out document took 2**26 steps - 66 seconds on a contract small enough to read by eye. The check meant to protect CI would have hung it. Memoising validated refs (the DFS stack still detects cycles) took the contract suite from 66s to 3.5s. Also fixed a classification bug: introducing maxLength where there was none was reported as merely risky because an absent bound was treated as unknown. An absent bound is the unbounded END of the range, so introducing one rejects every longer request that used to be valid - it is now correctly BREAKING.",
    "SDK PARITY IS EVIDENCE, NOT A CLAIM. contracts/sdk/capabilities.json lists nine operations and every one is exercised against the real app: Python via in-process uvicorn on loopback, TypeScript via scripts/serve_for_parity.py. The TS suite previously only asserted the requests it BUILT, which cannot catch a well-formed request the server rejects. CI sets AGENTROUTER_REQUIRE_LIVE=1 so a skipped live suite fails rather than passing quietly (verified: skips cleanly without it, exits 1 with it).",
    "VERIFICATION: 775 passed / 3 skipped; branch coverage 85.32% (gate 80%), contract.py 92%; 33 hook tests; ruff check + format clean; bandit 0 under -c pyproject.toml with no new nosec; pip-audit clean on the env and requirements.txt; the CI secret scan mirrored locally with 0 findings outside tests/; pytest -m security green; wheel + sdist built; contract export from a CLEAN WHEEL INSTALL IN AN UNRELATED DIRECTORY is BYTE-IDENTICAL to the committed contract; a core-only install (no [server] extra) gives guidance and exit 1, not a traceback, and the core CLI is unaffected; npm typecheck clean and 18/18 TS tests with the live app, 0 skipped; npm package scoped to src+README (7 files -> 3). Critical-module mutation gate re-run locally: PASS - overall 0.9858, safety_policy_execution 0.9877, routing_engine 0.9815, 0 unreviewed survivors, no threshold lowered and no new allowlist entry.",
    "SECOND REVIEW ROUND on the complete diff (PR #9 was already green on every CI job; green CI was not accepted as evidence - both reviewers reproduced their findings and so did I). Security: 2 HIGH, 2 MEDIUM, 3 LOW. HIGH-1: /ready is unauthenticated AND rate-limit exempt and logged the registry error at ERROR with a traceback; Python's lastResort handler writes ERROR to stderr with no handler configured, so ~8 KB per request of registry path, offending source line and any pasted credential went to the operator's log, with no auth and no rate limit. Fixed with log_api_error(detail=False) for user-data errors: measured 8016 -> 202 bytes/request, no path, no content, still actionable. HIGH-2: _SECRET_RE matched 4 vendor prefixes and 16 of 23 real credential formats passed through (Google, GitLab, Slack, HuggingFace, fine-grained GitHub PATs, AWS temporary keys, JWTs, DSN passwords, password= pairs); log_event skipped non-string values entirely. Replaced with a generic keyword+entropy detector plus recursive redact_value; all 23 masked, ordinary diagnostics unchanged.",
    "GATE BYPASS CLOSED: deleting contracts/http/v1/openapi.json turned four breaking changes into a green gate - the commit message had claimed the history was non-erasable, which was false. CI now also checks the live app against the contract on the BASE BRANCH, which the PR author does not control (verified: vs base -> endpoint_removed breaking; vs self-regenerated -> clean). Found while fixing it: `contract export` treated an UNREADABLE baseline as an absent one, so corrupting the file bypassed the same refusal deleting it triggers - exit 0 and silent overwrite. Split into BaselineMissing vs ContractError; absent/deeply-nested/truncated/dangling-ref all now behave correctly.",
    "COMPATIBILITY RE-REVIEW found the checker still had two high-impact FALSE GREENS: _compare_schema never descended into anyOf/oneOf/allOf - which is what FastAPI emits for EVERY optional field - so an enum value could be deleted or a constraint tightened with CI green; and components.securitySchemes was never compared, so renaming the auth header X-API-Key -> Authorization or switching apiKey -> http bearer was invisible. Both fixed and pinned by tests. Also three FALSE REDS corrected: a new required RESPONSE field, a second accepted auth scheme (OR semantics), and a request gaining null were all reported breaking; `required` is now read per direction, because on a request it is an obligation on the caller and on a response it is a guarantee from the server. A gate that cries wolf gets routed around.",
    "REGRESSION I INTRODUCED AND FIXED: typing the four new response models made /v1/decisions/{id} return 500 on a persisted decision whose engine payload shape had drifted. Decisions are opaque JSON in SQLite with no schema version, so asserting today's shape over yesterday's data breaks data the user already has. Reproduced, then fixed by typing engine-owned fields Any - the field NAMES are what the checker protects and they are unchanged.",
    "BOUNDS MADE REAL: _MAX_EXPANDED_NODES was a per-call budget while the traversal re-entered it per property, so cost still grew with API size - 73s from an 11 KB crafted file, scaling linearly with path count. One budget now lives on Report and is shared across the whole comparison, with an independent _compare_schema depth ceiling: measured 73s -> 0.20s, flat from 1 to 256 paths. _safe_write gained O_NOFOLLOW and an st_nlink refusal (a hardlink is invisible to is_symlink() and resolves inside the root); my own new test then caught a bug in that fix - O_TRUNC emptied the victim before the link check ran - so truncation now happens only after. Echoed decision_id bounded to 64 chars and stripped of control/bidi/zero-width characters: 5057 -> 122 bytes, CR/LF and U+202E removed, d_00001 untouched.",
    "BANDIT NOW SCANS scripts/ (mutation runner + parity server: they spawn processes and bind a socket). It immediately found a HIGH B613:trojansource in my own new sanitising regex, because I had written the bidi ranges as literal characters - rewritten as escapes. That finding is itself the argument for widening the scan.",
    "DOCUMENTATION CORRECTED RATHER THAN DEFENDED: AD-5 no longer claims the acceptance history is unerasable - it states plainly that contracts/ is a committed text file, that code review protects it, and that the control which does not depend on good behaviour is the base-branch check in CI. AD-6 added (why `required` is directional and why engine-owned payloads stay Any). docs/API.md gained a 'What the checker still cannot see' section (semantics, response headers, path-level parameters, webhooks/callbacks, discriminator, readOnly/writeOnly, schema-valued additionalProperties, multi-branch unions of differing arity) and documents that contracts/http/v1/ versions the ARTIFACT, not the API - there is one app and one document, and a v2 directory would describe the same app twice while orphaning the v1 baseline; a real v2 is versioned in the URL space. The unreachable `unavailable` 503 row was removed and CHANGELOG.md records the registry_unavailable change as breaking for anyone matching the old code.",
    "FINAL BATTERY: 821 passed / 3 skipped; branch coverage 85.29% (gate 80%); 33 hook tests; ruff check + format clean across 316 files; bandit 0 issues over agentrouter AND scripts; pip-audit clean; credential-pattern scan 0 outside tests/; pytest -m security green; wheel + sdist built; clean-wheel export in an unrelated directory BYTE-IDENTICAL to the committed contract; installed-wheel contract check clean; installed-wheel server smoke (health/ready/models/route/dry-run/404/X-Request-ID) all correct; core-only install exits 4 with guidance, not a traceback, and the core CLI still works; npm typecheck clean; 18/18 TS tests against the live app with 0 skipped and 0 temp dirs leaked; npm pack 3 files / 2.8 kB with nothing published.",
    "THIRD REVIEW ROUND: the compatibility reviewer re-checked the complete diff and found my own second-round fix was INCOMPLETE. `prompt` was still str|None, so /v1/decisions/{id} still returned 500 for a decision persisted when prompt was a dict, list or int. I had fixed the fields I thought of and missed one. The rule now admits no exceptions: every field on a _Passthrough envelope is Any; models we actually construct (HealthResponse, ModelSummary, HostStatusResponse, FeedbackResponse) keep real types. Verified across nine drift shapes: 0 non-200, live payload still 14 keys.",
    "ELEVEN MORE FALSE GREENS AND SEVEN FALSE REDS, all reproduced against the real exported contract and all fixed: enum INTRODUCED on a free-form request field was undetected (the comparison required an enum on both sides) - the most extreme narrowing there is; `const` was never compared although pydantic emits it for Literal; allOf was treated as a union so every allOf change was classified BACKWARDS (a conjunction gets narrower with more branches); type erasure to untyped was invisible; OpenAPI 3.1 list-form `type` was invisible; requestBody.required false->true was invisible; oauth2 `flows` was not compared; additionalProperties-as-a-schema counted as additive; request `format` was only risky while `pattern` was breaking. False reds: union branch reorder reported two breaking type changes (now canonicalised away), request union widening, response enum narrowing, 4XX refined into explicit codes, and allOf branch removal. The enum block was the ONLY classifier in _compare_schema that ignored the request/response direction - directly contradicting AD-6's own stated principle.",
    "THE ACCEPTANCE WORKFLOW WAS A DEAD END, and I had built it without testing it end to end. `contract check` never READ accepted_breaking_changes - it was only ever written. So export --accept-breaking moved the branch baseline and turned the in-branch check green, while the base-branch check I had just added still reported the same break forever: a change could be accepted and could never merge. `check` now reads the manifest in the tree under review and downgrades a break matching a recorded (kind, location) to a new `accepted` severity - reported prominently, does not fail. Proven end to end: routine export refuses (1) -> owner accepts with a reason (0) -> in-branch check passes -> BASE-BRANCH check passes -> a different unrecorded break in the same change still fails (1).",
    "DOCS CORRECTED AGAIN: AD-6 claimed 'the union is what gets compared' as the reason not to type engine payloads, but _unwrap was added in the same change precisely so that is no longer true - the claim is removed and the reason stands without it. docs/API.md band table rewritten with the explicit direction rules and the new `accepted` band; the 'cannot see' list corrected (covered categories removed; `not`, parameter style/explode/deprecated and security-scope granularity added). The CI job comment claimed it fails on any break 'without an owner-reviewed decision recorded in the contract manifest' - false until this round, true now.",
    "THIRD-ROUND BATTERY: 839 passed / 3 skipped; branch coverage 85.53%; 33 hook tests; ruff clean over 316 files; bandit 0 across agentrouter and scripts; pip-audit clean; credential scan 0 outside tests/; pytest -m security green; wheel + sdist; clean-wheel export in an unrelated directory BYTE-IDENTICAL; installed-wheel contract check clean; npm typecheck clean; 18/18 TS tests against the live app, 0 skipped, 0 temp dirs leaked; npm pack 3 files / 2.8 kB, nothing published.",
    "GAP RECORDED HONESTLY: the independent SECURITY re-review hit a session limit before returning its findings. The second-round security fixes are covered by their own regression tests, but no fresh adversarial security pass was completed against the final diff. Noted in the task progress rather than papered over."
  ],
  "completed_iter28": [
    "EPIC TASK-018 COMPLETE across three merged PRs. 018B (PR #10 -> ff06910) built a repo-native reliability lab and found FIVE real defects, each reproduced before being fixed: (1) 200 concurrent POST /v1/route returned 3x HTTP 500 sqlite3 'database is locked' - no WAL, no busy_timeout, and SQLite's default busy timeout is ZERO; this does NOT reproduce through TestClient at 40 concurrent, only against a real loopback server at 200, so a TestClient-only harness would have called the API healthy. (2) The WAL fix introduced a silent backup hazard: a plain cp of agentrouter.db reports 0 rows with no error - fixed with store.snapshot via SQLite's online backup API. (3) An unbounded task field was a disk-fill primitive - 2MB in produced a 4,004,603-byte response and a 4,005,888-byte row from one caller with no limit; fixed with bounded request fields, an OWNER-APPROVED breaking change recorded as 8 constraint_tightened acceptances. (4) Idempotent POSTs were not idempotent: 12 concurrent same-key same-body requests produced 6 distinct decisions because get/put were each locked but the window between them was not; fixed with per-key single-flight. (5) CI caught what the dev machine could not - WAL + 5s timeout sufficed locally but not on slower 3.10/3.11/Windows runners, so writers are now serialised in-process (SQLite takes one writer anyway, so queueing beats racing); verified at double CI concurrency, 400 requests / 64 workers, zero errors.",
    "FIRST REAL USE OF THE TASK-018A ACCEPTANCE WORKFLOW. The bounded request fields required a deliberate breaking change; the checker caught all eight tightenings and refused the routine export exactly as designed. Recording them with the measurement as justification made `contract check` report '8 owner-accepted' instead of failing - including against the RC's own contract, which is the base-branch check that would otherwise have blocked an accepted change forever. The owner was asked before merging rather than self-approved.",
    "TWO CI REPAIRS, NEITHER BY WEAKENING ANYTHING. The mutation gate failed with 8 unreviewed survivors, all inside the new IdempotencyCache.flight/release_flight - allowlisting would have been wrong because these were live mutants in new concurrency logic attacking exactly the properties the fix depends on (returning a fresh lock per call, or dropping a lock still held). Killed with six real unit tests; safety_policy_execution 0.9772 -> 0.9879. Separately test-windows failed on a Unix-only `resource` import in the soak profile - made optional, since this project deliberately targets Windows first.",
    "018C (PR #11 -> d81ad94) unified the three existing doctors WITHOUT reimplementing them: agentrouter/diagnostics.py calls the same primitives (hosts.detect_host, catalog_ops.read_status, registry.load_providers, store.connect) and returns structured results. A check never raises (three deliberately-broken installs produce no traceback) and never prints a secret. Warnings deliberately do NOT fail - open local mode with no API key is the DOCUMENTED default, so exiting non-zero on a fresh healthy install would cry wolf on the happy path. A set-but-blank API key IS a failure: it rejects every request, the same class of bug as TASK-016's blank-credential host.",
    "SECRET-SAFE DIAGNOSTIC BUNDLE (doctor --bundle) built from an ALLOWLIST, never a directory glob - a glob picks up whatever is sitting there, including a .env. Ten tests each attack one way something private could get in: a .env and credentials.json in the data directory are absent and their contents appear nowhere; the API key value is never recorded; task text is excluded unless --include-database, which makes the README say so; the database goes through store.snapshot rather than cp; an existing destination is refused rather than clobbered; a symlinked destination is refused with the target untouched.",
    "REDACTION MADE SECURE AND READABLE. The widened catch-all over-matched file paths - 'Projects/Agentrouteros/agentrouter/server/app' is 44 chars of [A-Za-z0-9+/], so paths vanished from tracebacks exactly where they matter most. The discriminator is a DIGIT: path segments are words, opaque tokens are not, and every realistic slash-containing secret (AWS secret access key, base64 blob) carries a digit. Only the catch-all was narrowed. Both directions asserted together: 16 credential formats still masked, four real paths preserved, plus an end-to-end traceback that keeps its paths and loses its key.",
    "TWELVE RUNBOOKS, EACH LABELLED BY EVIDENCE: Verified (with the backing test named), Local procedure, or Hypothetical. Eight are Verified and cite their test so a reader can check what 'verified' means. Production incident response is marked Hypothetical and says plainly that this project has no hosted deployment, no shared database and no on-call rotation - inventing backup rotation or capacity planning would be fabricating operational experience it does not have. tests/test_runbooks.py keeps the citations honest.",
    "THE MISSING TASK-018A SECURITY RE-REVIEW WAS CLOSED, not quietly dropped. An adversarial pass was executed over the merged surfaces: baseline tampering, $ref bombs (self-referential/remote/dangling all rejected), schema-complexity DoS (refused in 0.00s), unauthenticated /ready amplification (202 bytes/request, no path, no secret, no traceback), redaction, malicious identifiers (traversal, 9KB id, CRLF - no leak, no injected header), symlink AND hardlink write targets (all refused, victims intact), and parity-server temp resources (loopback only, cleaned). No new critical or high findings. The honest caveat is recorded: I ran it myself, so it is reproducible but not independent.",
    "GRAPHIFY INSTALLED AND PROVEN, then used. uv and pipx were absent and pip --user is PEP 668-blocked, so graphifyy 0.9.47 went into a dedicated ~/.venvs/graphify linked into ~/.local/bin - no sudo, no pollution of the project venv. Graph built with AST-only extraction (no LLM, no API key, no paid inference): 3623 nodes / 7084 edges / 309 communities from the final RC. Verified rather than trusted: save_decision and all four reported callers checked line-by-line against source. It earned its keep - `graphify affected \"connect\"` exposed that the WAL change reaches 8 CLI commands, 3 service functions and an evaluator, which prompted 'what else assumes this is one file?' and led directly to the silent-backup defect.",
    "DURABLE CROSS-SESSION MEMORY INSTALLED: CLAUDE.md (short auto-loaded bootstrap importing AGENT_HANDOFF.md and AGENTS.md, with the guardrails and the NTFS read-only hazard) and AGENT_HANDOFF.md (canonical live checkpoint - branch, commit, RC, PR, open findings, graph state, latest gates, exact next action). CODEX_HANDOFF.md now states its relationship to the canonical handoff rather than competing with it.",
    "NEXT MILESTONE SELECTED BY GRAPH-DRIVEN GAP ANALYSIS, not by picking a convenient cleanup. TASK-019 plugin installer hardening. Three independent signals agree: plugins.py is 818 statements at 67.7% branch coverage (largest under-tested module by a wide margin; next is evaluation/cli.py at 129 statements); four plugin symbols are in the graph's top-14 hubs; and it is the ONLY module writing into directories outside the project and removing files from them, while its docstring makes exactly the class of safety claim that 018A/B/C repeatedly found true in intent and incomplete in practice."
  ]
}
