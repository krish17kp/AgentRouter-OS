# Customer review — TASK-006

Developer-facing package. sdk/typescript/README.md has install + usage + method list +
error handling example + Node>=22 note. Zero runtime deps; ships as source. Parity with the
Python SDK gives JS/TS consumers the same ergonomics. PASS.
