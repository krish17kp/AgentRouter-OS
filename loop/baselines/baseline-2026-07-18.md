# Baseline — 2026-07-18

Captured before any behavior change this session (command.md STEP 3).

| Check | Command | Result |
|-------|---------|--------|
| Tests | `python -m pytest -q` | **371 passed**, 1 warning (starlette/httpx deprecation), 45.3s |
| Lint | `ruff check agentrouter` | clean (exit 0) |
| Format | `ruff format --check agentrouter` | clean (exit 0) |
| Eval | `agentrouter.evaluation.cli run --all` | see `../reports/` (running) |

- Repo SHA at session start: `602321a` (per git log: "Security hardening, Bandit, pip-audit and secret scanning completed")
- Python: `.venv` (project venv)
- Prior recorded baseline (iter 8, 2026-07-14): 371 passed, eval 98.08/100, wheel 0.4.0.

Environment note: one deprecation warning from FastAPI TestClient (starlette recommends
`httpx2`). Non-blocking; tracked as informational in KNOWN_LIMITATIONS.
